// ===============================================================
// Placeholder — the platform no longer shows a side-by-side
// "Correct Solution" column in the PDF, because with live question
// refresh the reference solution cannot be reliably pre-generated.
// Instead, the PDF shows the student's submitted code full-width with
// the per-line blue/yellow highlighting indicating what the student
// wrote (vs. unchanged starter). The instructor grades against the
// problem description shown immediately above each code block.
// ===============================================================

window.SOLUTIONS = {};
